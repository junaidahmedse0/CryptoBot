import React from 'react';

const Simulation = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Simulation</h1>
    </div>
  );
};

export default Simulation;
